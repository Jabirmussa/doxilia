import { dictionary as en } from './dictionary.en';
import { dictionary as pt } from './dictionary.pt';

export const dictionaries: Record<string, Record<string, string>> = {
  Portuguese: pt,
  English: en,
};